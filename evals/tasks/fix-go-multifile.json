{
  "id": "fix-go-multifile",
  "description": "Fix related failures that require targeted edits in more than one source file.",
  "prompt": "Fix the failing checkout math tests.",
  "fixture": "fixtures/go-multifile",
  "script": "scripts/fix-go-multifile.json",
  "checks": {
    "command": "go test ./...",
    "expected_files_changed": ["discount.go", "tax.go"],
    "forbidden_files_changed": ["go.mod", "checkout_test.go"]
  }
}
